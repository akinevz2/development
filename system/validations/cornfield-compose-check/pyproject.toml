[tool.poetry]
name = "cornfield-compose-check"
version = "0.1.0"
description = "Temporary validation project for Cornfield compose checks"
authors = ["Copilot <copilot@example.com>"]
packages = []

[tool.poetry.dependencies]
python = "^3.13"
PyYAML = "^6.0.2"

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"
