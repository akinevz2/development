// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/debian
{
  // Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
  "build": {
    "dockerfile": "Dockerfile",
    "context": "..",
    "args": {
      "USERNAME": "kine"
    }
  },
  // Configure tool-specific properties.
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "redhat.vscode-yaml",
        "redhat.vscode-quarkus",
        "tamasfe.even-better-toml",
        "eamodio.gitlens",
        "editorconfig.editorconfig",
        "streetsidesoftware.code-spell-checker",
        "usernamehw.errorlens",
        "GitHub.copilot",
        "GitHub.copilot-chat",
        "scalameta.metals",
        "scala-lang.scala"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "files.eol": "\n",
        "terminal.integrated.defaultProfile.linux": "bash",
        "metals.sbtScript": "sbt",
        "metals.showInferredType": true
      }
    }
  },
  // Features are optional; keep this minimal and add only what you need later.
  "features": {
    "ghcr.io/devcontainers/features/java:1": {
      "version": "21",
      "jdkDistro": "open",
      "installMaven": true
    },
    "ghcr.io/devcontainers/features/rust:1": {
      "version": "latest",
      "profile": "default"
    }
  },
  // Use 'forwardPorts' to make a list of ports inside the container available locally.
  "forwardPorts": [],
  // Persistent cache mounts
  "name": "Development Workstation",
  "workspaceFolder": "/workspaces/development",
  // Use 'postCreateCommand' to run commands after the container is created.
  "postCreateCommand": "bash .devcontainer/post-create.sh && bash -lc 'if [ -n \"${SDKMAN_DIR:-}\" ] && [ -s \"$SDKMAN_DIR/bin/sdkman-init.sh\" ]; then source \"$SDKMAN_DIR/bin/sdkman-init.sh\" && (sdk install sbt 1.10.2 || true) && (sdk install scala 3.6.2 || true); else echo \"SDKMAN not found; skipping sbt/scala install.\"; fi'",
  // Environment variables
  "containerEnv": {
    "MY_CREDENTIALS": "/workspaces/development/.my-credentials"
  },
  "remoteEnv": {
    "dotfiles": "https://github.com/akinevz2/configs.git",
    "EDITOR": "code --wait",
    "SSH_AUTH_SOCK": "/tmp/ssh-agent.sock"
  },
  "containerUser": "kine",
  "remoteUser": "kine",
  "updateRemoteUserUID": true
}