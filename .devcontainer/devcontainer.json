// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/debian
{
  // Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
  "image": "mcr.microsoft.com/devcontainers/base:latest",
  // Configure tool-specific properties.
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "redhat.vscode-yaml",
        "redhat.vscode-quarkus",
        "tamasfe.even-better-toml",
        "eamodio.gitlens",
        "editorconfig.editorconfig",
        "streetsidesoftware.code-spell-checker",
        "usernamehw.errorlens",
        "GitHub.copilot",
        "GitHub.copilot-chat"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "files.eol": "\n",
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  // Features are optional; keep this minimal and add only what you need later.
  "features": {
    "ghcr.io/devcontainers/features/java:1": {
      "version": "25",
      "jdkDistro": "tem",
      "installMaven": true
    }
  },
  // Use 'forwardPorts' to make a list of ports inside the container available locally.
  "forwardPorts": [],
  // Persistent cache mounts
  "mounts": [
    "source=${localEnv:HOME}/.config/gh,target=/home/vscode/.config/gh,type=bind,readonly",
    "source=${localEnv:HOME}/.ssh/id_ed25519.pub,target=/workspaces/development/kine_wsl_id_ed25519.pub,type=bind,readonly",
    "source=${localEnv:HOME}/.ssh/devcontainer-agent.sock,target=/tmp/ssh-agent.sock,type=bind"
  ],
  "initializeCommand": "bash -lc 'mkdir -p \"${HOME}/.config/gh\" \"${HOME}/.ssh\"; AGENT_SOURCE=\"\"; if [ -n \"${SSH_AUTH_SOCK:-}\" ] && [ -S \"${SSH_AUTH_SOCK}\" ]; then AGENT_SOURCE=\"${SSH_AUTH_SOCK}\"; else AGENT_SOURCE=$(find /tmp -maxdepth 3 -type s -path \"/tmp/ssh-*/agent.*\" 2>/dev/null | head -n 1); fi; if [ -n \"${AGENT_SOURCE}\" ] && [ -S \"${AGENT_SOURCE}\" ]; then ln -snf \"${AGENT_SOURCE}\" \"${HOME}/.ssh/devcontainer-agent.sock\"; else : > \"${HOME}/.ssh/devcontainer-agent.sock\"; fi'",
  "name": "Development Workstation",
  "workspaceFolder": "/workspaces/development",
  // Use 'postCreateCommand' to run commands after the container is created.
  "postCreateCommand": "bash .devcontainer/post-create.sh",
  // Environment variables
  "containerEnv": {
    "MY_CREDENTIALS": "/workspaces/development/.my-credentials",
    "GH_AUTH_ON_CREATE": "0",
    "GH_AUTH_PROVIDER": "wsl-host"
  },
  "remoteEnv": {
    "dotfiles": "https://github.com/akinevz2/configs.git",
    "EDITOR": "code --wait",
    "SSH_AUTH_SOCK": "/tmp/ssh-agent.sock"
  },
  "remoteUser": "vscode",
  "updateRemoteUserUID": true
}