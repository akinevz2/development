# Git Configuration

When this devcontainer is rebuilt or git commands are needed, configure git with these credentials:

```bash
git config --global user.email "akinevz@outlook.com"
git config --global user.name "Kirill <kine> Nevzorov"
```

These are the default credentials for commits in this repository.
